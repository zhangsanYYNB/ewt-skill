# 升学e网通 · 暑假任务看课自动化操作手册（历史参考）

> **注意：当前可执行规则以 `skills/ewt360-lessons/SKILL.md` / `skills/ewt360-lessons-mini/SKILL.md` 为准。新版禁止使用共享 `ewt360-progress.md`，登录前强制勾选隐私协议，必须把“点击加载更多”循环到“没有更多必学任务了”，并按 `tab list → close 一个 → tab list` 复核标签清理。本文件保留原始机制说明，不应直接作为新 run recipe。**
>
> 目标：把"语数英物化生暑假学习专题"任务（homeworkId=10493931）中未完成的**视频课**，一课一课播放到完成，使系统计数 +1。
> 本手册是给"能力较弱"的模型照做的标准工序，**每一步都要按顺序、串行执行，只发一个工具调用，等结果回来再发下一个**。

---

## 0. 铁律（违反任何一条都会翻车）

1. **一次只能发一个工具调用**，等结果返回后再发下一个。严禁把多个相同/不同的调用放进同一个回复里（会互相竞争、错过检测弹窗、浪费 token）。
2. **禁止用 JS 脚本直接推进视频、禁止用脚本调用 `.click()` 模拟点击**——站点有反脚本设计，会判"错过检测/非正常学习"。
3. 观看全程**必须用真实浏览器事件**点击（`click` 命令），唯一例外见第 3.2 节（2 倍速只能通过设置播放速率实现，这是站点自带功能、用户已授权的正常操作）。
4. **不要用固定 sleep 代替条件等待**。视频播放用"条件 wait"（检测弹窗 / 结束），见第 4 节。
5. 中途如果页面/会话异常，先冷静读状态（`eval` 读 currentTime / paused / 弹窗），**再决定下一步**，不要盲目重发。

---

## 1. 前置条件

- 已登录的浏览器会话（用 agent_browser 打开 ewt360.com 二维码登录即可）。
- 任务概览地址（固定）：
  `https://teacher.ewt360.com/ewtbend/bend/index/index.html#/holiday/student-task-overview?homeworkId=10493931`
- agent_browser 挂在固定 URL 上。若会话名太长报 socket 错，给命令加 `--session ewt`（短名字）。

---

## 2. 核心机制（先理解，再动手）

| 概念 | 说明 |
|---|---|
| 一门课的三个子任务 | 每门课有 `导`（导学）、`学`（正课视频）、`练`（练习）。**我们只刷 `学` 视频**。 |
| `学` 按钮 | 概览页课程项里 `data-type="2"` 的那个 `div`。点它 = 以"正确 lessonId"进入播放页。 |
| 完成判定 | 视频以 2 倍速播放到结尾（`currentTime >= duration - 3`），服务端即登记完成，概览计数 +1。 |
| 随机"认真度检测" | 播放中随机弹出，**24 秒内**要点"点击通过检查"，否则弹"你错过了本节课所有的互动检测"→ **该课不计数，必须重看**。 |
| ❌ 大坑 | **不要**在播放页右侧课程列表里点击切换下一课——那样 lessonId 上下文错乱，视频虽然播放但不会计数（已实测）。进课一律从概览点 `学`。 |
| ❌ 大坑 | 部分课在列表末尾，需要先点"点击加载更多"才能看到。 |

---

## 3. 进入一门课（标准动作）

### 3.1 打开概览并展开学科列表

```
open https://teacher.ewt360.com/ewtbend/bend/index/index.html#/holiday/student-task-overview?homeworkId=10493931
```

（若目标学科列表没展开）点击学科 tab：

```
click xpath=//li[contains(.,'语文') and contains(.,'完成')]
```

（把"语文"换成当前要做的学科名，如 数学/英语/物理/化学/生物。）

若目标课在列表末尾看不到，先点：

```
click xpath=//*[contains(text(),'点击加载更多') or contains(text(),'加载更多')]
```

### 3.2 点"学"按钮进入该课

用**精确标题**替换占位（标题用它页面上的完整文本，如 `T文言文实词对称邻字推断法`）：

```
click xpath=//li[contains(@class,'taskItem') and contains(.,'T文言文实词对称邻字推断法')]//div[@data-type='2']
```

> 标题必须包含 `T` 前缀（如 `T...`）可与其它课区分；若报 "Element not found"，说明列表没加载完，先做 3.1 的展开/加载更多，或等一下再试。

### 3.3 等视频就绪 + 开 2 倍速

等视频元素可用（单次 wait）：

```
wait --fn "(() => { const v = document.querySelector('video'); return v && v.readyState > 0; })()" --timeout 30000
```

设 2 倍速并确保播放（单次 eval，这是唯一允许的脚本操作，等价于播放器的 2X 倍速菜单，用户已确认）：

```
eval --stdin
(() => { const v = document.querySelector('video'); if (!v) return 'NOVID'; v.playbackRate = 2; v.play(); return {id: location.hash.match(/lessonId=(\d+)/)?.[1], t: Math.round(v.currentTime), d: Math.round(v.duration)}; })()
```

> 目标：返回里 `id` 必须是当前这门课的 lessonId、`d` 与课时长吻合、`t` 在增长。若 `NOVID`，先回 3.3 第一步再等。

---

## 4. 播放监控循环（看完全程）

**用同一个 wait 条件循环**，它只返回三类值，据此分支：

```
wait --fn "(() => {
  const d = document.querySelector('[class*=earnest_check]');
  if (d && d.offsetParent !== null) return 'CHECK';
  const v = document.querySelector('video');
  if (!v) return 'GONE';
  if (v.currentTime >= v.duration - 3) return 'END';
  return false;
})()" --timeout <见下方公式>
```

- **超时公式**（给足余量）：`(duration - currentTime) / 2 + 60` 秒。例如 15 分钟视频 ≈ (900-0)/2+60 = 510 秒。若中途 CHECK 过几次，下一次 wait 的 timeout 要按当前剩余时间重算。
- 返回 **CHECK**（检测弹窗出现，必须最快处理在家里）：
  ```
  click [class*=earnest_check] .btn-DOCWn
  ```
  弹窗按钮文案要么是"点击通过检查"、要么是"我知道了"，类名都是 `btn-DOCWn`，同一个选择器通用。点完回到第 4 步继续 wait。
  > 注意：若此刻弹的是"你错过了本节课所有的互动检测"= 已经错过所有检测，**这课重看**（见第 6 节）。
- 返回 **END**：视频放完 → 去第 5 步验证。
- 返回 **GONE**（video 消失了）：页面崩/被刷新 → 回到第 3 步重进这课。
- 若 **wait 超时但非 GONE**：说明视频卡住了（常见：检测弹窗出现后没点掉、或 2 倍速解码跟不上）。先单次 eval 读 `{t, dur, p, dlg}`：
  - `paused=true` 且有弹窗 → 点掉弹窗（`.btn-DOCWn`）；`paused=true` 无弹窗 → `v.play()` 恢复（playbackRate 会沿用）；
  - `paused=false` 但 `t` 不再增长 → 低端设备 2 倍速解码跟不上，降为 1 倍速（`v.playbackRate=1`）再等；
  - 处理完进入第 4 步继续。

---

## 5. 验证完成（每课必做）

回到概览读取学科计数（单次 open + 单次 eval，分两步）：

```
open https://teacher.ewt360.com/ewtbend/bend/index/index.html#/holiday/student-task-overview?homeworkId=10493931
```

```
eval --stdin
(() => {
  const items = [...document.querySelectorAll('li')];
  return items.map(e => e.innerText.replace(/\s+/g,' ').trim())
    .filter(t => t.includes('完成') && t.includes('/'))
    .slice(0, 6).join(' || ');
})()
```

返回形如 `语文 完成28/35 || 数学 完成11/27 ...`。

- **计数相比上一课 +1 = 成功**，进入下一课（重复第 3~5 节）。
- **计数不变 = 失败**，原因二选一：
  1. 点的是播放页右侧列表切换（没走概览 `学` 按钮）→ 按正确流程重来；
  2. 错过了互动检测 → 见第 6 节重看。

---

## 6. 错过检测后的重看流程

弹窗提示"你错过了本节课所有的互动检测，如需要补上，可以重看视频后再次触发检测：我知道了"时：

1. 先点掉弹窗：`click [class*=earnest_check] .btn-DOCWn`
2. **从头重看才能重新触发检测**：回到概览，重新点这门课的 `学` 按钮（第 3 节），视频会从 0 开始，检测会再次随机出现。
3. 这次**必须在 CHECK 一出现就单次点掉**（第 4 步循环，最快响应）。
4. 播完 → 第 5 步验证计数 +1。

---

## 7. 当前任务清单（2026 初始读取，供核对）

| 学科 | 初始进度 | 备注 |
|---|---|---|
| 语文 | 17/35 | 已完成刷到 27/35（语法结构、画面描绘、四步法、观点印证、比较鉴赏、哲理思辨、精妙构思、品析人物、综合效果等）|
| 数学 | 11/27 | 未开始 |
| 英语 | 2/20 | 未开始 |
| 物理 | 7/36 | 未开始 |
| 化学 | 5/27 | 未开始 |
| 生物 | 13/29 | 未开始 |

> 每做一个，去概览确认对应学科 "+1"。数字以概览页实时为准，不要依赖记忆。

---

## 8. 通用避坑速查

| 现象 | 处理 |
|---|---|
| 多个调用一起发导致异常/浪费 | 严格执行"一次一个调用" |
| 播放页右侧列表点了没用 | 一律回概览点 `学` 按钮 |
| 点不到目标课 | 先点学科 tab；末尾的课先点"加载更多" |
| 选不到倍速按钮（控制条被 video 盖住） | 用 eval 设 `playbackRate=2`（合法倍速） |
| 检测弹窗出现 | 立即 `click [class*=earnest_check] .btn-DOCWn`（24 秒窗口）|
| 提示"错过所有检测" | 重看（第 6 节）|
| wait 超时 | eval 查 {t,dur,p,dlg}，paused→点弹窗/play；卡顿→降 1 倍速 |
| 页面中途回会话长名报错 | 命令带 `--session ewt` |

---

## 9. 每门课的最低套餐（为了防止少动作，写死顺序）

```
1. open 概览
2. click 学科tab    (xpath=//li[contains(.,'<学科>') and contains(.,'完成')])
3. (若需要) click 加载更多
4. click 该课"学"按钮 (xpath=//li[contains(@class,'taskItem') and contains(.,'<精确标题>')]//div[@data-type='2'])
5. wait 视频就绪 (video.readyState>0)
6. eval 设 2 倍速 + play
7. [循环] wait CHECK/END → CHECK则click .btn-DOCWn后回本步；END则下一步；GONE则回第4步；超时则eval诊断后回本步
8. open 概览 → eval 读计数，确认 +1
9. 下一门/下一课
```

按此套餐，字段都给了具体选择器和超时公式，即使模型理解力一般，只要严格串行照做也能持续推进。
