You are executing the ewt360-lessons-mini fixed recipe with agent_browser only.
ACCOUNT=<YOUR_ACCOUNT> PASSWORD=<YOUR_PASSWORD> SUBJECT=<SUBJECT>.

RULES:
1 Browser actions are serial. Never use bash/read/write for page or progress. Never read or write ewt360-progress.md. Live overview counters are the only source of truth.
2 Every new account starts with open https://www.ewt360.com/ and sessionMode=fresh. Forget all prior account URLs, counters, titles and lessonIds.
3 Login order is fixed: fill account; fill password; check .privacy__agreement input[type='checkbox']; eval verify checked===true; only then click #login__password button[type='submit']. A separate visible 同意并继续 popup is clicked before the form if present.
4 Discover task fresh: open #/student/homework; click BOTH exact 全部 buttons; click first exact 查看详情; wait URL **/holiday/student-task-overview?homeworkId=*; get url = runtime OVERVIEW_URL.
5 Read SUBJECT counter BEFORE from live overview.
6 Open SUBJECT. Mandatory load loop: eval exact leaf text. If 点击加载更多/加载更多 exists, remember row count, real-click it, wait row count grows or 没有更多必学任务了, repeat. Do not select any lesson until exact 没有更多必学任务了 is observed. MISSING is not DONE.
7 Extract rows li[class*=taskItem] that contain div[data-type='2'] and do not contain 已学完. Pick the shortest unless user named one.
8 Tab cleanup is always: tab list; preserve active intended tab; tab close ONE non-active stable id; immediately tab list again. The close command may return Tab not found after actually closing, so only the following list decides success. Never use batch --bail for tab cleanup.
9 Enter lesson only by overview XPath //li[contains(@class,'taskItem') and contains(.,'<EXACT_TITLE>')]//div[@data-type='2']. Never use player right-side playlist. Wait player URL with lessonId. Run the same close-one/list loop until only active player remains; get url verifies it.
10 Wait video, set playbackRate=2 and play. Never seek currentTime and never use JS .click().
11 Monitor via raw batch wait+real-click. batch cannot inspect results or branch; --bail only stops on failure. Use fixed sequential wait CHECK/MISSED/GONE/END then click [class*=earnest_check] .btn-DOCWn. Read details.batchSteps[0] and branch in the NEXT tool call. On END, second-click failure is benign. If batch stdin validation fails once, use standalone wait; CHECK must be followed immediately by the real click.
12 On END reopen OVERVIEW_URL and read live SUBJECT counter. Success only if AFTER=BEFORE+1. Unchanged means rewatch same title from overview, max two retries. Never skip.
13 Do not save account, URL, counter, title, lessonId, or run state to any file.

For exact commands/selectors, follow the loaded skills/ewt360-lessons-mini/SKILL.md. Final report contains masked account, title, lessonId, and before->after only.
